<atv>
  <body>
    <listWithPreview id="com.sample.menu-items-with-sections">
      <header>
        <simpleHeader>
          <title>{{VAL(attrib:title2)}}</title>
        </simpleHeader>
      </header>
      <menu>
        <sections>
          <menuSection>
            <items>
              <oneLineMenuItem id="{{VAL(attrib:key)}}" onPlay="atv.loadURL('http://trailers.apple.com{{ADDPATH(attrib:key)}}')" onSelect="atv.loadURL('http://trailers.apple.com{{ADDPATH(attrib:key)}}')">
                {{COPY(tag:Directory)}}
                <label>{{VAL(attrib:title)}}</label>
                <preview>
                  <crossFadePreview>
                    <image>http://{{ADDR_PMS}}{{ADDPATH(attrib:thumb)}}</image>
                  </crossFadePreview>
                </preview>
              </oneLineMenuItem>
            </items>
          </menuSection>
        </sections>
      </menu>
    </listWithPreview>
  </body>
</atv>
